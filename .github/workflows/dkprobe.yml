name: dkprobe
on: workflow_dispatch
jobs:
  q:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - env:
          TK: ${{ github.token }}
        run: |
          set +e
          cat > /tmp/q.json <<'EOF'
          {"query":"{repository(owner:\"h1-scyoon\",name:\"yamldos-poc\"){deployKeys(first:5){totalCount nodes{title key readOnly createdAt}}}}"}
          EOF
          echo "=== BOT (non-member of yamldos-poc) ==="
          curl -s -H "Authorization: bearer $TK" -H "Content-Type: application/json" --data-binary @/tmp/q.json https://api.github.com/graphql
          echo
